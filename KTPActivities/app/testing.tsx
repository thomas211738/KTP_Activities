import React from 'react';
import { Redirect } from 'expo-router';

// Reviewer accounts authenticate through the same Google flow as members.
export default function Testing() { return <Redirect href="/" />; }
