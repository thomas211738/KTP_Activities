# Root .env.example (monorepo wrapper)
# Most configuration lives inside KTPActivities/.env and backend/.env
# Copy this if any root-level scripts ever need shared vars.

# Example only – usually not needed for running the app or backend.
# BACKEND_URL=http://10.0.0.155:5000
